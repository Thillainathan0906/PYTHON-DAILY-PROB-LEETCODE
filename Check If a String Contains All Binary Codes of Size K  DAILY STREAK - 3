Given a binary string s and an integer k, return true if every binary code of length k is a substring of s. Otherwise, return false.

 

Example 1:

Input: s = "00110110", k = 2
Output: true
Explanation: The binary codes of length 2 are "00", "01", "10" and "11". They can be all found as substrings at indices 0, 1, 3 and 2 respectively.
Example 2:

Input: s = "0110", k = 1
Output: true
Explanation: The binary codes of length 1 are "0" and "1", it is clear that both exist as a substring. 
Example 3:

Input: s = "0110", k = 2
Output: false
Explanation: The binary code "00" is of length 2 and does not exist in the array.
 

Constraints:

1 <= s.length <= 5 * 105
s[i] is either '0' or '1'.
1 <= k <= 20


ANSWER :
class Solution:
    def hasAllCodes(self, s: str, k: int) -> bool:
        # 🔴 Step 1: If string length is smaller than k → impossible
        if len(s) < k:
            return False
        
        # 🟡 Step 2: Create empty set to store unique substrings
        seen = set()
        
        # 🔵 Step 3: Take all substrings of length k
        for i in range(len(s) - k + 1):
            substring = s[i:i+k]
            seen.add(substring)
        
        # 🟢 Step 4: Check if count matches 2^k
        return len(seen) == 2**k

APPROACH;
✅ Problem Idea (Simple Words)

Unakku:

s → binary string (0,1 matum)

k → length

Check panna vendiyathu:

👉 All possible binary codes of length k s kulla irukka?

If irundha → True
Illana → False


So namma:

s la length k substrings ellam collect pannuvom

Unique ah set la store pannuvom

Set size == 2^k na → answer True

CODE EXPLAIN :
🎨 Code Explanation (Color Meaning)

🔴 Step 1 – Base Condition
If string length < k → we cannot even form one substring.

🟡 Step 2 – Create Set
Set removes duplicates automatically.

Example:
["01", "11", "01"]
Becomes:
{"01", "11"}
🔵 Step 3 – Sliding Window
We take every substring of size k.

Example:

s = "00110110"
k = 2
Substrings:

00
01
11
10
01
11
10

🟢 Step 4 – Final Check

Unique substrings:

{"00", "01", "11", "10"}

Count = 4

Since:

2^2 = 4

Answer → ✅ True

⏱ Time Complexity
O(n)

Only one loop over string.

🎯 Final Logic (One Line Thinking)
If unique k-length substrings == 2^k → True
Else → False
